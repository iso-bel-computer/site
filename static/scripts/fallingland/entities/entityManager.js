import { WaterManager } from './water.js';

export class EntityManager {
    // constructor(grid) {
    //     this.grid = grid
    //     this.water = new WaterManager(grid)
    // }

    // addWaterSource(tile) {
    //     this.water.addOrigin(tile)
    // }

    // tick(tickNumber) {
    //     this.water.tick(tickNumber)
    // }

}
